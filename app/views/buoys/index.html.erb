<div class="container">
  <div class="row">
    <div class="col-8">
    <br>
      <h1>Choisis ta bouée:</h1>
      <div class="cards">
        <% @buoys.each do |buoy| %>
          <div class="card-trip">
            <div class="card-trip-image">
              <% if buoy.photo.attached? %>
                <%= image_tag buoy.photo, alt: "bouée", width: 300, heigth: 200, crop: :fit %>
              <% end %>
            </div>
            <div class="card-trip-infos">
              <div class="name">
                <h2><%= buoy.name %></h2>
                <p>📍 <%= buoy.address %></p>
                <%= link_to "Voir détails", buoy_path(buoy), data: { turbolinks: false } %>
              </div>
              <h3 class="card-trip-pricing"><%= buoy.price %>€ / jour</h3>
            </div>
          </div>
        <% end %>
      </div>
    </div>

    <% if user_signed_in? %>
      <div class="add-button">
    </div>
          <%= link_to "Ajoute une bouée", new_buoy_path, class: "btn-create"%>
      </div>
    <% end %>

    <div class="col-4">
      <div id="map"
          style="width: 100%; height: 450px;"
          data-markers="<%= @markers.to_json %>"
          data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
      </div>
    </div>
<br>
</div>
